/*
core mod

load    dotfiles
save    dotfiles
add     dotfiles
remove  dotfiles

manage config


*/

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
